use crate::engine_generator;

engine_generator!(
    avg_die,
    equipment_format,
    lowercase,
    markdown,
    multi_class_req_gen,
    starting_prof_format,
    feature_table_list,
    has_cantrips,
    cantrips_known,
    spell_slots,
    has_spells,
    max_spell_slot
);
